const userAuthSchema = require("./userAuth");
const productSchema = require("./product");
const imageSchema = require("./imageScheam");

module.exports = {
  userAuthSchema,
  productSchema,
  imageSchema
};